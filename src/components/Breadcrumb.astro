---
export interface Props {
  items: Array<{ label: string; href?: string }>;
}

const { items } = Astro.props;
const base = import.meta.env.BASE_URL || '/';
---

<nav aria-label="面包屑导航" class="mb-4">
  <ol class="flex items-center gap-1.5 text-sm text-gray-500 dark:text-gray-400">
    <li>
      <a href={base} class="hover:text-poker-gold transition-colors">首页</a>
    </li>
    {items.map((item, i) => (
      <li class="flex items-center gap-1.5">
        <span aria-hidden="true">/</span>
        {item.href ? (
          <a href={item.href} class="hover:text-poker-gold transition-colors">{item.label}</a>
        ) : (
          <span class="text-gray-900 dark:text-gray-200 font-medium">{item.label}</span>
        )}
      </li>
    ))}
  </ol>
</nav>
